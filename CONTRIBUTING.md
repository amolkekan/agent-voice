# Contributing

Node 18+. Run `npm test` after changes. Keep MCP stdout as one JSON object per line (no `Content-Length` headers). Host-specific notes belong in `README.md` or `VOICE.md`, not in the server.
